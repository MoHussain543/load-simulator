# Optional backend API base URL.
# Leave unset during `npm run dev` — Vite proxies /api to http://localhost:8080.
# Set for `vite preview` or when the frontend is served separately from the backend:
# VITE_API_BASE=http://localhost:8080
